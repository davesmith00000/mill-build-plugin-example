//| mill-version: 1.0.6

package build

import mill.*
import mill.scalalib.*

/** Run `./mill demo.speak` from the command line. */
object demo extends _root_.myplugin.Greeter {
  def scalaVersion = "3.7.1"
}
