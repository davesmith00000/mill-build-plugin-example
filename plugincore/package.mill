package build.plugincore

import mill.*
import mill.scalalib.*
import mill.scalalib.publish.*

import build.Shared
import build.SharedPublish

object `package` extends Shared, SharedPublish:
  override def scalaVersion = "3.7.1"

  def mvnDeps = Seq(
    mvn"io.circe::circe-core:${millbuild.DepVersions.circeVersion}"
  )

  def description = "Core utilities for the example Mill plugin."
